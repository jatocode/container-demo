version: "3"
services:
  client:
    build:
      context: ./client
    image: client:latest
    ports:
      - "3000:3000"
  api:
    build:
      context: ./DemoAPI
    image: api:latest
    ports:
      - "5111:80"
